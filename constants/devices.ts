import { sizes } from './sizes'

export const devices = {
  mobile: `(min-width: ${sizes.mobile})`,
  tablet: `(min-width: ${sizes.tablet})`,
  desktop: `(min-width: ${sizes.desktop})`,
  ultraDesktop: `(min-width: ${sizes.ultraDesktop})`
}
